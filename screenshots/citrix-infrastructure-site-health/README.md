# Screenshots

Screenshots are intentionally not required for installation.

The repository keeps the importable ADX dashboard JSON and reusable KQL as the
primary artifacts. Add sanitized screenshots here only when they do not expose
customer, host, user, session, network, or infrastructure identifiers.