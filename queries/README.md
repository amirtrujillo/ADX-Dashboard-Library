# The queries Folder

This folder is generated from dashboard JSON files in `source/adx`.

Each dashboard receives:

- `tiles/` — KQL used by dashboard tiles.
- `parameters/` — KQL used to populate dashboard parameters.
- `README.md` — generated query index.

Do not edit generated KQL directly. Update the dashboard source and rebuild.
